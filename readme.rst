React Hooks Chat
================

.. image:: chat.png

Demo
----

`Demo <http://reactchat.surge.joelburton.com>`_

Running
-------

.. parsed-literal::

  $ `cd backend`:cmd:
  $ `npm install`:cmd:
  $ `npm start`:cmd:

.. parsed-literal::

  $ `cd frontend`:cmd:
  $ `npm install`:cmd:
  $ `npm start`:cmd:

